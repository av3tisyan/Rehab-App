# Normalize line endings to LF in the repo (prevents CRLF breaking shell scripts,
# Dockerfiles, and Caddyfile when the project is checked out on the Linux server).
* text=auto eol=lf

# Binary types git should never touch.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
